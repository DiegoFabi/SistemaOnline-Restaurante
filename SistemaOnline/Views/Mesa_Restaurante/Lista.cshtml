@model List<SistemaOnline.ViewModels.Mesa_RestauranteVM>
@{
    Layout = "_DashboardLayout";
    ViewData["Title"] = "Mesas";
    ViewBag.Active = "negocio";
    ViewData["Titulo"] = "Lista de Mesas";
    ViewData["Descripcion"] = "Administra las mesas del restaurante.";
    ViewData["Controlador"] = "Mesa_Restaurante";
    ViewData["NuevoTexto"] = "Nueva Mesa";
    ViewData["BuscadorPlaceholder"] = "Buscar por número, ubicación, estado...";
}
<partial name="_CRUDListLayout" />
<div class="bg-surface-container-lowest rounded-xl shadow-sm border border-outline-variant/20 overflow-hidden">
    <div class="p-lg overflow-x-auto">
        <table id="tablaPrincipal" class="w-full text-left">
            <thead class="text-label-md text-on-surface-variant uppercase tracking-wider">
                <tr>
                    <th class="pb-md font-bold">ID</th>
                    <th class="pb-md font-bold">Número de Mesa</th>
                    <th class="pb-md font-bold">Capacidad</th>
                    <th class="pb-md font-bold">Ubicación</th>
                    <th class="pb-md font-bold">Estado</th>
                    <th class="pb-md font-bold">Acciones</th>
                </tr>
            </thead>
            <tbody class="text-body-md">
                @foreach (var item in Model)
                {
                    <tr class="hover:bg-slate-50 transition-colors border-b border-outline-variant/10">
                        <td class="py-md font-mono-data">@item.ID_Mesa</td>
                        <td class="py-md font-bold">@item.Numero_Mesa</td>
                        <td class="py-md font-mono-data">@item.Capacidad</td>
                        <td class="py-md">@item.Ubicacion</td>
                        <td class="py-md">@item.Estado</td>
                        <td class="py-md">
                            <div class="flex items-center gap-md">
                                <a asp-controller="Mesa_Restaurante" asp-action="Editar" asp-route-id="@item.ID_Mesa" class="inline-flex items-center gap-xs text-tertiary hover:underline">
                                    <span class="material-symbols-outlined text-[18px]">edit</span> Editar
                                </a>
                                <a asp-controller="Mesa_Restaurante" asp-action="Eliminar" asp-route-id="@item.ID_Mesa" onclick="return confirm('¿Estás seguro de eliminar este registro?');" class="inline-flex items-center gap-xs text-error hover:underline">
                                    <span class="material-symbols-outlined text-[18px]">delete</span> Eliminar
                                </a>
                            </div>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>
</div>
